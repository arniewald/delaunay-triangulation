import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

from functions.reading_functions import read_anemia_data, read_diabetes_data, read_general_data


data, labels, dim = read_diabetes_data(features = ['Insulin','Glucose','DiabetesPedigreeFunction'])
print(len(data))
fig = plt.figure()
ax = fig.add_subplot(projection='3d')
colors=[]
for label in labels:
    if label==1:
        colors.append('r')
    else:
        colors.append('b')
ax.scatter3D(data[:,0],data[:,1],data[:,2],color=colors)
plt.show()

